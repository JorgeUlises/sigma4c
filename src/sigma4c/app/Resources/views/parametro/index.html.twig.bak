{% extends 'base.html.twig' %}

{% block body %}
    <h1>Parametro list</h1>

    <table>
        <thead>
            <tr>
                <th>Nombre</th>
                <th>Unidad</th>
                <th>Id</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
        {% for parametro in parametros %}
            <tr>
                <td><a href="{{ path('parametro_show', { 'id': parametro.id }) }}">{{ parametro.nombre }}</a></td>
                <td>{{ parametro.unidad }}</td>
                <td>{{ parametro.id }}</td>
                <td>
                    <ul>
                        <li>
                            <a href="{{ path('parametro_show', { 'id': parametro.id }) }}">show</a>
                        </li>
                        <li>
                            <a href="{{ path('parametro_edit', { 'id': parametro.id }) }}">edit</a>
                        </li>
                    </ul>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('parametro_new') }}">Create a new entry</a>
        </li>
    </ul>
{% endblock %}
