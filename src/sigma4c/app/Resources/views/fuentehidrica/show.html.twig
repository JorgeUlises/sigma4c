{% extends 'sigmabase.html.twig' %}

{% block body %}
    <h1>FuenteHidrica</h1>

    <table>
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ fuenteHidrica.id }}</td>
            </tr>
            <tr>
                <th>Geometria</th>
                <td>{{ fuenteHidrica.geometria }}</td>
            </tr>
            <tr>
                <th>Nombre</th>
                <td>{{ fuenteHidrica.nombre }}</td>
            </tr>
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('fuentehidrica_index') }}">Back to the list</a>
        </li>
        <li>
            <a href="{{ path('fuentehidrica_edit', { 'id': fuenteHidrica.id }) }}">Edit</a>
        </li>
        <li>
            {{ form_start(delete_form) }}
                <input type="submit" value="Delete">
            {{ form_end(delete_form) }}
        </li>
    </ul>
{% endblock %}
